import { HStack, Button } from '@chakra-ui/react'

const App = () => {
  return (
    <HStack>
      <Button colorPalette='blue'>Click!!</Button>
      <Button colorPalette='blue' variant="surface">Click!!</Button>
    </HStack>
  )
}

export default App